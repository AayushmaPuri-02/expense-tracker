<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top px-3">
  <div class="container-fluid d-flex align-items-center justify-content-start">
      <!-- Logo and Title Section -->
      <div class="d-flex flex-column align-items-start me-5"> <!-- Added more margin -->
          <a class="navbar-brand" href="/">
            <i class="fa-solid fa-sack-dollar"></i>
          </a>
          <span class="fw-bold fs-5 text-white">Expense Tracker</span>
      </div>

      <!-- Navigation Buttons - Aligned Left -->
      <div class="d-flex gap-5"> <!-- Increased gap between buttons -->
          <a class="btn btn-outline-light" href="/">Home</a>
          <a class="btn btn-outline-light" href="/expenses">All Expenses</a>
          <a class="btn btn-outline-warning" href="/expenses/new">+ Add Expense</a>
      </div>

      <div class="d-flex gap-5 ms-auto"> <!-- Increased gap between buttons -->
        <% if (!currUser) { %>
          <a class="btn btn-outline-light" href="/signup">Sign Up</a>
          <a class="btn btn-outline-light" href="/login">Log in</a>
        <% } else { %>
          <a class="btn btn-outline-warning" href="/logout">+ Log out</a>
        <% } %>
    </div>
  </div>
</nav>